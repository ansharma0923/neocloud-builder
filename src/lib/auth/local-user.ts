/**
 * Local development user identity.
 * This app runs as a single-user local tool — no authentication required.
 * All data is scoped to this user ID which is seeded on first run.
 */
export const LOCAL_USER_ID = 'local-user-default';
export const LOCAL_USER_EMAIL = 'local@neocloud.local';
export const LOCAL_USER_NAME = 'Local User';
